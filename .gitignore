# macOS
.DS_Store

# Generated by XcodeGen — regenerate with `xcodegen generate`
*.xcodeproj

# Build artifacts
/build/
/DerivedData/
DerivedData/
.build/
Sapat.app/
*.iconset/

# Swift Package Manager
.swiftpm/
# Package.resolved is committed for reproducible CI builds.

# Xcode user state
xcuserdata/
*.xcuserstate

# Release signing — the ed25519 PRIVATE key must never be committed (goes to a GitHub secret).
release-signing-key.txt
